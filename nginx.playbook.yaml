- name: install and run nginx 
  hosts: worker
  become: yes
  tasks:
  - name: install nginx package
    ansible.builtin.dnf:
      name: nginx
      state: present
  - name: Run nginx
    ansible.builtin.service:
      name: nginx
      state: started  